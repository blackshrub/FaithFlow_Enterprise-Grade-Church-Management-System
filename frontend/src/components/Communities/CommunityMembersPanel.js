/**
 * Community Members Panel
 *
 * Wrapper around GroupMembersPanel for community terminology.
 */
import { GroupMembersPanel } from '../Groups/GroupMembersPanel';

export const CommunityMembersPanel = (props) => {
  return <GroupMembersPanel {...props} />;
};
