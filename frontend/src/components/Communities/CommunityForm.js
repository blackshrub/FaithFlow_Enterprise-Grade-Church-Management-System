/**
 * Community Form Component
 *
 * Wrapper around GroupForm for community terminology.
 */
import { GroupForm } from '../Groups/GroupForm';

export const CommunityForm = (props) => {
  return <GroupForm {...props} />;
};
