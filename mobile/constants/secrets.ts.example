/**
 * Secret API Keys Template
 *
 * SETUP:
 * 1. Copy this file to secrets.ts
 * 2. Replace placeholder values with your actual API keys
 *
 * Get keys from:
 * - Anthropic: https://console.anthropic.com/
 * - Google Cloud: https://console.cloud.google.com/apis/credentials
 * - Groq: https://console.groq.com/keys (free tier available)
 */

// Anthropic Claude API key for Faith Assistant
export const DEV_ANTHROPIC_API_KEY = __DEV__
  ? 'sk-ant-api03-YOUR_ANTHROPIC_KEY_HERE'
  : null;

// Google Cloud TTS API key
export const DEV_GOOGLE_TTS_API_KEY = __DEV__
  ? 'YOUR_GOOGLE_CLOUD_API_KEY_HERE'
  : null;

// Groq API key for fast STT (~10x faster than OpenAI)
export const DEV_GROQ_API_KEY = __DEV__
  ? 'gsk_YOUR_GROQ_KEY_HERE'
  : null;

// OpenAI API key (for STT fallback if Groq not available)
export const DEV_OPENAI_API_KEY = __DEV__
  ? 'sk-proj-YOUR_OPENAI_KEY_HERE'
  : null;
