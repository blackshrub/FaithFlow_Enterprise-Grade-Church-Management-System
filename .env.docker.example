# =============================================================================
# FaithFlow Docker Environment Configuration
# =============================================================================
#
# Copy this file to .env and configure for your environment:
#   cp .env.docker.example .env
#
# =============================================================================

# -----------------------------------------------------------------------------
# DOMAIN CONFIGURATION (Required for Production)
# -----------------------------------------------------------------------------

# Your primary domain (without protocol)
DOMAIN=faithflow.church

# Email for Let's Encrypt SSL certificate notifications
ACME_EMAIL=admin@faithflow.church

# -----------------------------------------------------------------------------
# SECURITY (Required)
# -----------------------------------------------------------------------------

# JWT Secret - Generate a secure random string (64+ characters)
# Generate with: openssl rand -base64 64 | tr -dc 'a-zA-Z0-9' | head -c 64
JWT_SECRET=your-super-secure-jwt-secret-change-this-in-production

# Traefik Dashboard Authentication (optional)
# Generate with: htpasswd -nb admin yourpassword
# Default: admin:admin (CHANGE THIS!)
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$ruca84Hq$$mbjdMZBAG.KWn7vfN/SNK/

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------

# MongoDB (internal docker network, no need to change)
MONGO_URL=mongodb://mongodb:27017
DB_NAME=faithflow

# -----------------------------------------------------------------------------
# OPTIONAL SERVICES
# -----------------------------------------------------------------------------

# AI Services (for content generation)
ANTHROPIC_API_KEY=
STABILITY_API_KEY=

# WhatsApp Gateway (for notifications)
WHATSAPP_API_URL=
WHATSAPP_USERNAME=
WHATSAPP_PASSWORD=

# Payment Gateway (iPaymu)
IPAYMU_VA=
IPAYMU_API_KEY=
IPAYMU_ENV=sandbox

# -----------------------------------------------------------------------------
# MOBILE APP
# -----------------------------------------------------------------------------

# Deep linking configuration
MOBILE_APP_SCHEME=faithflow://
IOS_APP_STORE_URL=https://apps.apple.com/app/faithflow
ANDROID_PLAY_STORE_URL=https://play.google.com/store/apps/details?id=com.faithflow
