# =============================================================================
# Git Attributes - Line Endings & LFS Configuration
# =============================================================================
# This file ensures consistent line endings across all platforms (Windows/Mac/Linux)
# Without this, git pull may show "local changes" even when you didn't change anything

# =============================================================================
# LINE ENDINGS - Enforce LF for text files
# =============================================================================

# Default: Auto-detect text files and normalize to LF
* text=auto eol=lf

# Shell scripts - MUST be LF (will break on Linux if CRLF)
*.sh text eol=lf

# Python files
*.py text eol=lf

# JavaScript/TypeScript
*.js text eol=lf
*.jsx text eol=lf
*.ts text eol=lf
*.tsx text eol=lf
*.json text eol=lf

# Config files
*.yml text eol=lf
*.yaml text eol=lf
*.toml text eol=lf
*.conf text eol=lf
*.env text eol=lf
.env.* text eol=lf
Dockerfile text eol=lf
docker-compose*.yml text eol=lf

# Web files
*.html text eol=lf
*.css text eol=lf
*.scss text eol=lf
*.md text eol=lf

# =============================================================================
# BINARY FILES - Don't modify these
# =============================================================================

# Images
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.svg binary

# Fonts
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary

# =============================================================================
# GIT LFS - Large File Storage
# =============================================================================

# TTS Model files
*.pth filter=lfs diff=lfs merge=lfs -text
*.pt filter=lfs diff=lfs merge=lfs -text

# Large data files
*.tar.gz filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text

# Bible JSON files are text but large
backend/data/bible/*.json filter=lfs diff=lfs merge=lfs -text
